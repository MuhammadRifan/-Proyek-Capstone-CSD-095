import 'package:flutter/material.dart';

class AddVetCare extends StatelessWidget {
  const AddVetCare({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("sada"),
      ),
      body: Container(
        child: Text("asda"),
      ),
    );
  }
}
